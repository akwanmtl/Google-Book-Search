import React, { Component } from 'react';
import API from '../utils/API';

class Saved extends Component {
  
  

  render() {
    return (
      <div className="container is-max-desktop">
        This will be the saved page
      </div>
    )
  }
  
}

export default Saved;